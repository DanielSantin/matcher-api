[Unit]
Description=Matcher API (product fuzzy matching)
After=network.target

[Service]
User=ubuntu
WorkingDirectory=/home/ubuntu/matcherApi
ExecStart=/home/ubuntu/matcherApi/venv/bin/uvicorn main:app --host 0.0.0.0 --port 8001
Restart=always
RestartSec=5

[Install]
WantedBy=multi-user.target
